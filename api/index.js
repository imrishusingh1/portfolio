// Vercel Serverless entry point
// Imports the Express app and exports it as the default handler.
// Vercel wraps this automatically as a serverless function.
import app from '../server/index.js'

export default app
